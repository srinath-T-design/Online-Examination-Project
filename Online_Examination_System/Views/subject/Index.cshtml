@model IEnumerable<Bussiness_Object_Layer.Model.subject>

@{
    ViewBag.Title = "Index";
    Layout = null;
}
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Colan Quiz</title>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.1.0/css/all.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <style>
        body {
            background-color: #d5fefd;
            background-image: linear-gradient(315deg, #d5fefd 0%, #fffcff 74%);
        }

        .container {
            margin-top: 50px;
        }

        a {
            color: white;
            text-decoration: none;
        }

        h3 {
            padding: 5px;
            border-radius: 5px;
            background-color: lightslategray;
            color: white;
        }

        h6 {
            background-color: lightseagreen;
            padding: 5px;
            color: whitesmoke;
            border-radius: 25px;
        }
    </style>



</head>


<body class="container">
    <h3>SubjectTitle:</h3>


    @*<p>
            @Html.ActionLink("Create New", "Create")
        </p>*@

    <table class="table">
        <tr>
            <h6>Subject Name:</h6>
            @*<th>

                  @Html.DisplayNameFor(model => model.subjectname)


                </th>*@
            @*<th></th>*@
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.subjectname)
                   
                </td>

                <td>
                    <button class="btn btn-sm btn-primary">
                        @*@Html.ActionLink("Edit", "Edit", new { id = item.subjectID }) |*@
                        @Html.ActionLink("View", "Details", new { id = item.subjectID })
                        <i class="fas fa-eye"></i>
                        @*@Html.ActionLink("Delete", "Delete", new { id = item.subjectID })*@
                        </button>
                </td>
            </tr>
        }

    </table>
</body>
</html>